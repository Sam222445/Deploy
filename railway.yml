build:
  env:
    PATH: $PATH:/app/.heroku/python/bin/

build:
  steps:
    - name: Install Gunicorn
      command: pip install gunicorn

build:
  steps:
    - name: Install Gunicorn
      command: pip install gunicorn

web: gunicorn pf03.wsgi
